<?php

declare(strict_types=1);

namespace Modules\Returns\Http\Controllers;

use App\Http\Controllers\Controller;

/**
 * ReturnDefinitionsController — Filament admin is the primary management surface.
 *
 * No Inertia pages are rendered here. CRUD for return definitions is handled
 * exclusively via the Filament admin panel (ReturnDefinitionResource).
 *
 * This controller exists as a placeholder to satisfy the route file and
 * to ensure the class is discoverable by the autoloader.
 */
class ReturnDefinitionsController extends Controller {}
